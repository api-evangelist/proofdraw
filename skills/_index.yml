generated: '2026-08-11'
method: generated
source: https://proofdraw.com/api
api: openapi/proofdraw-api-openapi.yml
note: >-
  ProofDraw publishes no AGENTS.md and no skill files of its own; these are packaged operating
  instructions generated by API Evangelist from the published OpenAPI and API reference. Every step is
  grounded in a real method+path from https://proofdraw.com/openapi.json — the spec declares no
  operationIds, so operations are referenced by method+path rather than by id (see
  overlays/proofdraw-api-overlay.yaml, which assigns stable ids non-destructively).
skills:
  - file: proofdraw-run-verifiable-draw.md
    name: Run a verifiable draw
    description: >-
      One-shot draw via POST /v1/draws/instant, with correct handling of the 60-second synchronous wait cap
      and the absence of any idempotency key.
    api: openapi/proofdraw-api-openapi.yml
    operations:
      - POST /v1/draws/instant
      - GET /v1/draws/{id}
      - GET /v1/me
      - POST /v1/draws/{id}/resolve
  - file: proofdraw-schedule-a-draw.md
    name: Schedule a draw and collect the result asynchronously
    description: >-
      Granular create/entries/seal flow for large or future-dated draws, taking the outcome from the
      HMAC-signed webhook rather than blocking on HTTP.
    api: openapi/proofdraw-api-openapi.yml
    operations:
      - POST /v1/draws
      - POST /v1/draws/{id}/entries
      - POST /v1/draws/{id}/seal
      - GET /v1/draws/{id}
      - POST /v1/draws/{id}/resolve
  - file: proofdraw-verify-a-draw.md
    name: Independently verify a ProofDraw result
    description: >-
      Public, credential-free verification — re-hash the sealed list, confirm the commitment predates the
      randomness via OpenTimestamps and the git mirror, and re-derive the winning row from drand.
    api: openapi/proofdraw-api-openapi.yml
    operations:
      - GET /list/{hash}
      - GET /list/{hash}/ots
      - GET /health
