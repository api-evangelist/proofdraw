# ProofDraw

Provably-fair random selection REST API that seals entry lists with SHA-256 commitments bound to future drand (League of Entropy) randomness rounds, producing publicly verifiable draw receipts with client-side open-source verification.

- **Provider:** https://proofdraw.com/
- **Documentation:** https://proofdraw.com/api
- **Base URL:** https://proofdraw.com/api/v1
- **Tags:** randomness, provably-fair, drand, verifiable-randomness, cryptography, raffle, giveaway, sweepstakes, lottery, verification, webhooks, rest-api
- **Public API:** yes
- **Agent-native (MCP / llms.txt / skills):** no
- **Confidence:** 95/100

## APIs

- **ProofDraw API** — Provably-fair random selection REST API (v1) covering draw lifecycle, account, health, and public verification artifacts. HMAC-signed webhooks. Bearer API-key auth. (`https://proofdraw.com/api/v1`)

> Live OpenAPI 3.0.3 JSON contract confirmed at https://proofdraw.com/openapi.json (title 'ProofDraw API', v1.0.0, server https://proofdraw.com/api, 13 paths). The apis.json advertises a stale/inaccessible openapi.yaml pointer (404) — catalog the JSON instead. Bearer API-key auth (pd_live_/pd_test_ prefixes); public verification endpoints (/health, /v/{id}, /list/{hash}, /list/{hash}/ots) require no auth. HMAC-signed webhooks (draw.sealed, draw.resolved, draw.cancelled). No agent-native surfaces found: no MCP server (hosted or local), no llms.txt, no published agent skills. Open-source client-side verifier at github.com/proofdraw/verifier (MIT). No AsyncAPI, GraphQL, or public Postman collection found.

_Generated by apis.io from a community submission._